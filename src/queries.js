const HTTP_REQUEST_QUERY = {
  element: 'httpRequest',
};

const HTTP_RESPONSE_QUERY = {
  element: 'httpResponse',
};

const ANNOTATION = {
  element: 'annotation',
};

export {
  HTTP_REQUEST_QUERY,
  HTTP_RESPONSE_QUERY,
  ANNOTATION,
};
